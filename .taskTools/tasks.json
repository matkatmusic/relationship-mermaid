[
  {
    "taskNumber": 1,
    "version": "c07f3b64fe01aa91e1f78f3c1f55053cdd47b048",
    "title": "Couples mobile app built on the issue pipeline diagram",
    "userDescription": "Convert this diagram into a mobile app for couples where they can log issues, and they both see where they are in the \"issue pipeline\".  The app uses this pipeline to navigate individual issues.  It has two modes: issue raiser, issue causer. Either side can raise issues, and the app tracks issues. App would allow creation of Impact statements, all the places where documenting thoughts could occur, journaling, timers for each side speaking, etc.  MVP would include allowing the user to scroll through the diagram and see where they are in the conflict process.  Fully-encrypted user data.  Future expansion: Affiliated with therapy platforms, video chatting with therapists.  Related to the voice tracker idea I had that listens in to a couple's fight and flags when someone breaks any agreed-upon fight rules (name-calling, shutting down, stonewalling).",
    "goal": [
      "- A user can scroll the diagram on a phone and see the current step marked",
      "- Either person can raise an issue and both see it",
      "- All user data is stored encrypted",
      "- A written spec exists before any app code",
      "- users use the clickable-ness of the diagram to navigate a conflict with their person."
    ],
    "notInScope": [
      "- Affiliation with therapy platforms; named as future expansion, no task yet",
      "- Video chatting with therapists; named as future expansion, no task yet",
      "- The voice tracker that listens to a fight and flags broken fight rules; a related separate idea, no task yet"
    ],
    "problemSolvedByTask": "Couples lose track of where they are in resolving an issue",
    "description": "Repo is currently a small Bun app (server.js + index.html) that lets a person view/edit mermaid `.mmd` flowcharts and trace one path through them; it has no user accounts, no data storage beyond the raw diagram text files, no auth, and no mobile packaging of any kind. diagrams/accountability.mmd is the \"issue causer\" accountability pipeline the user means by \"issue pipeline\" — nodes like SELF_CREATE_SYNTHETIC_IMPACT_STATEMENT, Q_THEIR_IMPACT_STATEMENT, and SELF_CHECK_EMPATHY_GOAL/SELF_CHECK_REGRET_GOAL/SELF_CHECK_FOLLOW_UP_GOAL already encode the impact-statement and empathy/regret/follow-up stages the request describes; diagrams/say-something-or-let-it-go.mmd and diagrams/stop-causing-harms.mmd are separate, smaller flowcharts in the same repo. index.html's parseEdges/walkTrail functions already do path-highlighting through a diagram in the browser, which is the closest existing building block to the requested MVP (\"scroll through the diagram and see where you are\"), but it runs in a desktop browser tab, not a mobile app, and has no concept of a second user, a raiser/causer role, or persisted per-couple state. The request asks for a native/mobile app, per-user encrypted data, two-role issue tracking, journaling, speaking timers, and (as future work) audio monitoring and therapist video chat — none of which has any code, chosen framework, backend, or auth/encryption library anywhere in this repo, so no concrete file list can be given without first deciding a mobile stack (e.g. React Native/Expo vs. native) and a backend, which the request does not specify. This is effectively a new product built on top of the diagram-viewing idea rather than a bounded change to the existing viewer.",
    "modifiableFiles": [],
    "readOnlyFiles": [
      "*"
    ],
    "schemaVersion": "1.0.1",
    "hasTests": false,
    "tests": "",
    "difficulty": 10
  },
  {
    "taskNumber": 2,
    "version": "7f47399cb65f7c640b6043ba2855a9e85f478129",
    "title": "Issue-raiser check-ins and decision-point break timer",
    "userDescription": "the issue-raiser side should be focused on asking \"do you feel heard by your partner?\" and \"Do you feel your partner understands the issue and impact on you?\".  one of the ways it answers these questions is by checking in with the issue-raiser as the couple navigates the diagram and works through decision points during the conflict.  Separately, the app monitors how long the couple has remained on a decision point and sets time limits so that it can ask \"do you need to take a break from the discussion?\"",
    "chainGoal": [
      "- A couples app that walks both people through the issue pipeline diagram; tasks 1 and 2",
      "- Task 1 builds the app and the issue-causer side; task 2 adds the issue-raiser check-ins and the break timer"
    ],
    "goal": [
      "- The issue-raiser is asked \"do you feel heard?\" at check-in points",
      "- The issue-raiser is asked \"does your partner understand the issue and impact?\"",
      "- Time on a decision point is tracked and a limit triggers the break prompt",
      "- A written spec covers this before any app code"
    ],
    "notInScope": [
      "- The issue-causer side; task 1 owns it",
      "- The full app build; task 1 owns it",
      "- The voice tracker; separate idea, no task"
    ],
    "problemSolvedByTask": "The issue-raiser has no way to say they still feel unheard",
    "description": "index.html is the only place diagram interaction currently happens: parseEdges/walkTrail/highlightPath track a single chosenAnswers Set as a user clicks through Q_-prefixed decision nodes, with no concept of two people, roles, or elapsed time on a node. There is no \"issue-raiser\" vs \"issue-causer\" role anywhere in the code (diagrams/accountability.mmd is written entirely from one person's \"Me\" perspective) and no existing check-in, prompt, or timer UI to build on. Q_*-prefixed diamond nodes across diagrams/*.mmd are the natural existing mapping for \"decision points.\" Delivering this needs: a role selector so the app knows who the issue-raiser is, a check-in prompt surfaced as walkTrail's active node advances, and a per-node timer (started when a node becomes the bright/active node in highlightPath) that fires a break-suggestion prompt once a configurable time limit is reached. None of the role, check-in, or timer concepts exist in server.js either, and this can be built as client-only state in index.html without server or diagram-file changes. .taskTools/tasks.json task 1 (\"Couples mobile app...\") is the umbrella idea that also expects raiser/causer roles and per-side timers, but this task can be scoped to the existing single-page web viewer instead of waiting on a mobile rebuild.",
    "modifiableFiles": [
      "index.html"
    ],
    "readOnlyFiles": [
      "*"
    ],
    "schemaVersion": "1.0.1",
    "hasTests": true,
    "tests": "Stay on one decision point past the limit, see the break prompt",
    "difficulty": 8,
    "blockedBy": [
      {
        "taskNumber": 1,
        "reason": "Task 1 builds the couples mobile app itself (spec, mobile framework/backend, issue-raiser/issue-causer modes, and diagram-navigation state tracking) and none of that exists yet in the repo (modifiableFiles is empty, readOnlyFiles is \"*\", no spec written). This task's check-in prompts and decision-point timers are features layered on top of that app's raiser mode and diagram-navigation state, so they have nothing to attach to until task 1 produces the app/spec."
      }
    ]
  },
  {
    "taskNumber": 3,
    "version": "a43426ebc903ebf908a192da18210f611c053c64",
    "title": "Check-in notifications and calendar reminders for scheduled loop-backs",
    "userDescription": "the mobile app would have notifications for the check-ins as well as the ability to integrate with calendar reminders so stuff that gets scheduled actually gets scheduled",
    "chainGoal": [
      "- A couples app that walks both people through the issue pipeline diagram; tasks 1, 2, and 3",
      "- Task 1 builds the app; task 2 adds issue-raiser check-ins and the break timer; task 3 adds notifications and calendar reminders"
    ],
    "goal": [
      "- A check-in sends a phone notification at its time",
      "- A scheduled loop-back time creates an event in the phone calendar",
      "- Both partners get the reminder",
      "- A written spec covers this before any app code"
    ],
    "notInScope": [
      "- The check-in questions themselves; task 2 owns them",
      "- The full app build; task 1 owns it",
      "- Therapist scheduling; future idea, no task"
    ],
    "problemSolvedByTask": "Couples agree to come back to an issue and then forget",
    "description": "Repo has no mobile app yet: task 1 (\"Couples mobile app...\") and task 2 (\"Issue-raiser check-ins and decision-point break timer\") are both still open in .taskTools/tasks.json with modifiableFiles: [] and readOnlyFiles: \"*\", meaning no mobile framework, backend, check-in UI, or scheduling concept exists in code. index.html only implements a desktop-browser mermaid diagram viewer (parseEdges/walkTrail/highlightPath) with no user accounts, no persisted state, and no notification or calendar code anywhere in the repo. server.js is a Bun HTTP server exposing only diagram CRUD endpoints (/api/diagrams, /api/watch) with no auth, user, or scheduling model to attach reminders to. This request adds push notifications for check-ins (which task 2 has not built yet) and two-way calendar-reminder integration (device calendar or a service like Google/Apple Calendar) so scheduled items actually create reminders, but no mobile stack, push-notification service, or calendar API has been chosen anywhere in the repo. No concrete file list can be given without those choices first, so this is new functionality layered on two not-yet-built features (the mobile app and the check-in system) rather than a bounded change to the existing diagram viewer.",
    "modifiableFiles": [],
    "readOnlyFiles": [
      "*"
    ],
    "schemaVersion": "1.0.1",
    "hasTests": true,
    "tests": "Agree a loop-back time, see it land on the phone calendar and get a notification",
    "difficulty": 10,
    "blockedBy": [
      {
        "taskNumber": 2,
        "reason": "Notifications for check-ins need the check-in feature to exist first, and task 2 is what builds issue-raiser check-ins; this task only adds notifications and calendar-reminder integration on top of that."
      }
    ]
  },
  {
    "taskNumber": 4,
    "version": "96831c634e06f35f96e4ae43b92c15e8bc1bb130",
    "title": "Help article for the \"Goal: Empathy\" block",
    "userDescription": "create a new task for each of the blocks that are the main goal points in the pipeline near the bottom, impact, empathy, change behavior, communication, there should be help articles that explain what those things are and what examples of them look like.",
    "chainGoal": [
      "- One help article per goal block in accountability.mmd, each linked from its block in the viewer"
    ],
    "goal": [
      "- The article says what the goal means in plain words",
      "- The article gives at least two real-life examples",
      "- The article gives an example of what it is NOT",
      "- The article is reachable from the block in the viewer"
    ],
    "notInScope": [
      "- Changes to the pipeline logic; the diagram stays as is",
      "- The mobile app; tasks 1 to 3 own it",
      "- Articles for the question diamonds; the question-diamonds article task owns them"
    ],
    "problemSolvedByTask": "The goal names are vague; people do not know what to actually do at each one",
    "description": "diagrams/accountability.mmd defines the goal blocks as node IDs SELF_CHECK_EMPATHY_GOAL, SELF_CHECK_IMPACT_GOAL, SELF_CHECK_REGRET_GOAL, SELF_APOLOGIZE, SELF_SPOT_MY_PATTERNS, SELF_CHECK_CHANGE_GOAL, SELF_EXPRESS_NEW_PATTERN, and SELF_CHECK_FOLLOW_UP_GOAL, plus roughly 19 Q_*-prefixed decision-diamond nodes. index.html renders via mermaid.render() and discards bindFunctions; its #output click listener only toggles Q_* answer nodes and never calls preventDefault, so a mermaid `click NODE_ID \"url\"` link directive (rendered as a native SVG <a> wrapping the node) navigates normally with no index.html changes needed. server.js serves only \"/\", \"/api/diagrams\", \"/api/diagrams/:name\", and \"/api/watch/:name\"; there is no static-file route, so a new articles/ directory needs its own route in server.js before article pages are reachable. This task writes articles/goal-empathy.html and adds `click SELF_CHECK_EMPATHY_GOAL \"articles/goal-empathy.html\"` to accountability.mmd. It also adds the static route for articles/ in server.js; the other article tasks depend on that route.",
    "modifiableFiles": [
      "diagrams/accountability.mmd",
      "articles/goal-empathy.html",
      "server.js"
    ],
    "readOnlyFiles": [
      "*"
    ],
    "createsFiles": [
      "articles/goal-empathy.html"
    ],
    "schemaVersion": "1.0.1",
    "hasTests": true,
    "tests": "Click the block in the viewer and the article opens",
    "difficulty": 5
  },
  {
    "taskNumber": 5,
    "version": "0641da1837ca1e73c3be5ec7e0ec7b2e868a679e",
    "title": "Help article for the \"Goal: Understand Impact\" block",
    "userDescription": "create a new task for each of the blocks that are the main goal points in the pipeline near the bottom, impact, empathy, change behavior, communication, there should be help articles that explain what those things are and what examples of them look like.",
    "chainGoal": [
      "- One help article per goal block in accountability.mmd, each linked from its block in the viewer"
    ],
    "goal": [
      "- The article says what the goal means in plain words",
      "- The article gives at least two real-life examples",
      "- The article gives an example of what it is NOT",
      "- The article is reachable from the block in the viewer"
    ],
    "notInScope": [
      "- Changes to the pipeline logic; the diagram stays as is",
      "- The mobile app; tasks 1 to 3 own it",
      "- Articles for the question diamonds; the question-diamonds article task owns them",
      "- The articles/ static route in server.js; task 4 owns it"
    ],
    "problemSolvedByTask": "The goal names are vague; people do not know what to actually do at each one",
    "description": "diagrams/accountability.mmd defines the goal blocks as node IDs SELF_CHECK_EMPATHY_GOAL, SELF_CHECK_IMPACT_GOAL, SELF_CHECK_REGRET_GOAL, SELF_APOLOGIZE, SELF_SPOT_MY_PATTERNS, SELF_CHECK_CHANGE_GOAL, SELF_EXPRESS_NEW_PATTERN, and SELF_CHECK_FOLLOW_UP_GOAL, plus roughly 19 Q_*-prefixed decision-diamond nodes. index.html renders via mermaid.render() and discards bindFunctions; its #output click listener only toggles Q_* answer nodes and never calls preventDefault, so a mermaid `click NODE_ID \"url\"` link directive (rendered as a native SVG <a> wrapping the node) navigates normally with no index.html changes needed. server.js serves only \"/\", \"/api/diagrams\", \"/api/diagrams/:name\", and \"/api/watch/:name\"; there is no static-file route, so a new articles/ directory needs its own route in server.js before article pages are reachable. This task writes articles/goal-understand-impact.html and adds `click SELF_CHECK_IMPACT_GOAL \"articles/goal-understand-impact.html\"` to accountability.mmd.",
    "modifiableFiles": [
      "diagrams/accountability.mmd",
      "articles/goal-understand-impact.html"
    ],
    "readOnlyFiles": [
      "*"
    ],
    "createsFiles": [
      "articles/goal-understand-impact.html"
    ],
    "schemaVersion": "1.0.1",
    "hasTests": true,
    "tests": "Click the block in the viewer and the article opens",
    "difficulty": 4,
    "blockedBy": [
      {
        "taskNumber": 4,
        "reason": "Needs the articles/ static route that the empathy article task adds to server.js"
      }
    ]
  },
  {
    "taskNumber": 6,
    "version": "a22441d5f849d6b96e50a692c4110be264c4ac14",
    "title": "Help article for the \"Goal: Feel Regret\" block",
    "userDescription": "create a new task for each of the blocks that are the main goal points in the pipeline near the bottom, impact, empathy, change behavior, communication, there should be help articles that explain what those things are and what examples of them look like.",
    "chainGoal": [
      "- One help article per goal block in accountability.mmd, each linked from its block in the viewer"
    ],
    "goal": [
      "- The article says what the goal means in plain words",
      "- The article gives at least two real-life examples",
      "- The article gives an example of what it is NOT",
      "- The article is reachable from the block in the viewer"
    ],
    "notInScope": [
      "- Changes to the pipeline logic; the diagram stays as is",
      "- The mobile app; tasks 1 to 3 own it",
      "- Articles for the question diamonds; the question-diamonds article task owns them",
      "- The articles/ static route in server.js; task 4 owns it"
    ],
    "problemSolvedByTask": "The goal names are vague; people do not know what to actually do at each one",
    "description": "diagrams/accountability.mmd defines the goal blocks as node IDs SELF_CHECK_EMPATHY_GOAL, SELF_CHECK_IMPACT_GOAL, SELF_CHECK_REGRET_GOAL, SELF_APOLOGIZE, SELF_SPOT_MY_PATTERNS, SELF_CHECK_CHANGE_GOAL, SELF_EXPRESS_NEW_PATTERN, and SELF_CHECK_FOLLOW_UP_GOAL, plus roughly 19 Q_*-prefixed decision-diamond nodes. index.html renders via mermaid.render() and discards bindFunctions; its #output click listener only toggles Q_* answer nodes and never calls preventDefault, so a mermaid `click NODE_ID \"url\"` link directive (rendered as a native SVG <a> wrapping the node) navigates normally with no index.html changes needed. server.js serves only \"/\", \"/api/diagrams\", \"/api/diagrams/:name\", and \"/api/watch/:name\"; there is no static-file route, so a new articles/ directory needs its own route in server.js before article pages are reachable. This task writes articles/goal-feel-regret.html and adds `click SELF_CHECK_REGRET_GOAL \"articles/goal-feel-regret.html\"` to accountability.mmd.",
    "modifiableFiles": [
      "diagrams/accountability.mmd",
      "articles/goal-feel-regret.html"
    ],
    "readOnlyFiles": [
      "*"
    ],
    "createsFiles": [
      "articles/goal-feel-regret.html"
    ],
    "schemaVersion": "1.0.1",
    "hasTests": true,
    "tests": "Click the block in the viewer and the article opens",
    "difficulty": 4,
    "blockedBy": [
      {
        "taskNumber": 4,
        "reason": "Needs the articles/ static route that the empathy article task adds to server.js"
      }
    ]
  },
  {
    "taskNumber": 7,
    "version": "b3ea7ffcb8aece46babe241843a8651c03f147cc",
    "title": "Help article for the \"Me: apologize\" block",
    "userDescription": "create a new task for each of the blocks that are the main goal points in the pipeline near the bottom, impact, empathy, change behavior, communication, there should be help articles that explain what those things are and what examples of them look like.",
    "chainGoal": [
      "- One help article per goal block in accountability.mmd, each linked from its block in the viewer"
    ],
    "goal": [
      "- The article says what the goal means in plain words",
      "- The article gives at least two real-life examples",
      "- The article gives an example of what it is NOT",
      "- The article is reachable from the block in the viewer"
    ],
    "notInScope": [
      "- Changes to the pipeline logic; the diagram stays as is",
      "- The mobile app; tasks 1 to 3 own it",
      "- Articles for the question diamonds; the question-diamonds article task owns them",
      "- The articles/ static route in server.js; task 4 owns it"
    ],
    "problemSolvedByTask": "The goal names are vague; people do not know what to actually do at each one",
    "description": "diagrams/accountability.mmd defines the goal blocks as node IDs SELF_CHECK_EMPATHY_GOAL, SELF_CHECK_IMPACT_GOAL, SELF_CHECK_REGRET_GOAL, SELF_APOLOGIZE, SELF_SPOT_MY_PATTERNS, SELF_CHECK_CHANGE_GOAL, SELF_EXPRESS_NEW_PATTERN, and SELF_CHECK_FOLLOW_UP_GOAL, plus roughly 19 Q_*-prefixed decision-diamond nodes. index.html renders via mermaid.render() and discards bindFunctions; its #output click listener only toggles Q_* answer nodes and never calls preventDefault, so a mermaid `click NODE_ID \"url\"` link directive (rendered as a native SVG <a> wrapping the node) navigates normally with no index.html changes needed. server.js serves only \"/\", \"/api/diagrams\", \"/api/diagrams/:name\", and \"/api/watch/:name\"; there is no static-file route, so a new articles/ directory needs its own route in server.js before article pages are reachable. This task writes articles/me-apologize.html and adds `click SELF_APOLOGIZE \"articles/me-apologize.html\"` to accountability.mmd.",
    "modifiableFiles": [
      "diagrams/accountability.mmd",
      "articles/me-apologize.html"
    ],
    "readOnlyFiles": [
      "*"
    ],
    "createsFiles": [
      "articles/me-apologize.html"
    ],
    "schemaVersion": "1.0.1",
    "hasTests": true,
    "tests": "Click the block in the viewer and the article opens",
    "difficulty": 4,
    "blockedBy": [
      {
        "taskNumber": 4,
        "reason": "Needs the articles/ static route that the empathy article task adds to server.js"
      }
    ]
  },
  {
    "taskNumber": 8,
    "version": "e0c2ec0c206499c7767eb8c22221d5515bfb8ae4",
    "title": "Help article for the \"Goal: Self-awareness\" block",
    "userDescription": "create a new task for each of the blocks that are the main goal points in the pipeline near the bottom, impact, empathy, change behavior, communication, there should be help articles that explain what those things are and what examples of them look like.",
    "chainGoal": [
      "- One help article per goal block in accountability.mmd, each linked from its block in the viewer"
    ],
    "goal": [
      "- The article says what the goal means in plain words",
      "- The article gives at least two real-life examples",
      "- The article gives an example of what it is NOT",
      "- The article is reachable from the block in the viewer"
    ],
    "notInScope": [
      "- Changes to the pipeline logic; the diagram stays as is",
      "- The mobile app; tasks 1 to 3 own it",
      "- Articles for the question diamonds; the question-diamonds article task owns them",
      "- The articles/ static route in server.js; task 4 owns it"
    ],
    "problemSolvedByTask": "The goal names are vague; people do not know what to actually do at each one",
    "description": "diagrams/accountability.mmd defines the goal blocks as node IDs SELF_CHECK_EMPATHY_GOAL, SELF_CHECK_IMPACT_GOAL, SELF_CHECK_REGRET_GOAL, SELF_APOLOGIZE, SELF_SPOT_MY_PATTERNS, SELF_CHECK_CHANGE_GOAL, SELF_EXPRESS_NEW_PATTERN, and SELF_CHECK_FOLLOW_UP_GOAL, plus roughly 19 Q_*-prefixed decision-diamond nodes. index.html renders via mermaid.render() and discards bindFunctions; its #output click listener only toggles Q_* answer nodes and never calls preventDefault, so a mermaid `click NODE_ID \"url\"` link directive (rendered as a native SVG <a> wrapping the node) navigates normally with no index.html changes needed. server.js serves only \"/\", \"/api/diagrams\", \"/api/diagrams/:name\", and \"/api/watch/:name\"; there is no static-file route, so a new articles/ directory needs its own route in server.js before article pages are reachable. This task writes articles/goal-self-awareness.html and adds `click SELF_SPOT_MY_PATTERNS \"articles/goal-self-awareness.html\"` to accountability.mmd.",
    "modifiableFiles": [
      "diagrams/accountability.mmd",
      "articles/goal-self-awareness.html"
    ],
    "readOnlyFiles": [
      "*"
    ],
    "createsFiles": [
      "articles/goal-self-awareness.html"
    ],
    "schemaVersion": "1.0.1",
    "hasTests": true,
    "tests": "Click the block in the viewer and the article opens",
    "difficulty": 4,
    "blockedBy": [
      {
        "taskNumber": 4,
        "reason": "Needs the articles/ static route that the empathy article task adds to server.js"
      }
    ]
  },
  {
    "taskNumber": 9,
    "version": "313cb642e06aec383077c84e5bc6eb6d39997446",
    "title": "Help article for the \"Goal: Changed behavior\" block",
    "userDescription": "create a new task for each of the blocks that are the main goal points in the pipeline near the bottom, impact, empathy, change behavior, communication, there should be help articles that explain what those things are and what examples of them look like.",
    "chainGoal": [
      "- One help article per goal block in accountability.mmd, each linked from its block in the viewer"
    ],
    "goal": [
      "- The article says what the goal means in plain words",
      "- The article gives at least two real-life examples",
      "- The article gives an example of what it is NOT",
      "- The article is reachable from the block in the viewer"
    ],
    "notInScope": [
      "- Changes to the pipeline logic; the diagram stays as is",
      "- The mobile app; tasks 1 to 3 own it",
      "- Articles for the question diamonds; the question-diamonds article task owns them",
      "- The articles/ static route in server.js; task 4 owns it"
    ],
    "problemSolvedByTask": "The goal names are vague; people do not know what to actually do at each one",
    "description": "diagrams/accountability.mmd defines the goal blocks as node IDs SELF_CHECK_EMPATHY_GOAL, SELF_CHECK_IMPACT_GOAL, SELF_CHECK_REGRET_GOAL, SELF_APOLOGIZE, SELF_SPOT_MY_PATTERNS, SELF_CHECK_CHANGE_GOAL, SELF_EXPRESS_NEW_PATTERN, and SELF_CHECK_FOLLOW_UP_GOAL, plus roughly 19 Q_*-prefixed decision-diamond nodes. index.html renders via mermaid.render() and discards bindFunctions; its #output click listener only toggles Q_* answer nodes and never calls preventDefault, so a mermaid `click NODE_ID \"url\"` link directive (rendered as a native SVG <a> wrapping the node) navigates normally with no index.html changes needed. server.js serves only \"/\", \"/api/diagrams\", \"/api/diagrams/:name\", and \"/api/watch/:name\"; there is no static-file route, so a new articles/ directory needs its own route in server.js before article pages are reachable. This task writes articles/goal-changed-behavior.html and adds `click SELF_CHECK_CHANGE_GOAL \"articles/goal-changed-behavior.html\"` to accountability.mmd.",
    "modifiableFiles": [
      "diagrams/accountability.mmd",
      "articles/goal-changed-behavior.html"
    ],
    "readOnlyFiles": [
      "*"
    ],
    "createsFiles": [
      "articles/goal-changed-behavior.html"
    ],
    "schemaVersion": "1.0.1",
    "hasTests": true,
    "tests": "Click the block in the viewer and the article opens",
    "difficulty": 4,
    "blockedBy": [
      {
        "taskNumber": 4,
        "reason": "Needs the articles/ static route that the empathy article task adds to server.js"
      }
    ]
  },
  {
    "taskNumber": 10,
    "version": "e1a438672d823de79f4165c5dece75c2399bda21",
    "title": "Help article for the \"Communicate: here is what I am going to do differently\" block",
    "userDescription": "create a new task for each of the blocks that are the main goal points in the pipeline near the bottom, impact, empathy, change behavior, communication, there should be help articles that explain what those things are and what examples of them look like.",
    "chainGoal": [
      "- One help article per goal block in accountability.mmd, each linked from its block in the viewer"
    ],
    "goal": [
      "- The article says what the goal means in plain words",
      "- The article gives at least two real-life examples",
      "- The article gives an example of what it is NOT",
      "- The article is reachable from the block in the viewer"
    ],
    "notInScope": [
      "- Changes to the pipeline logic; the diagram stays as is",
      "- The mobile app; tasks 1 to 3 own it",
      "- Articles for the question diamonds; the question-diamonds article task owns them",
      "- The articles/ static route in server.js; task 4 owns it"
    ],
    "problemSolvedByTask": "The goal names are vague; people do not know what to actually do at each one",
    "description": "diagrams/accountability.mmd defines the goal blocks as node IDs SELF_CHECK_EMPATHY_GOAL, SELF_CHECK_IMPACT_GOAL, SELF_CHECK_REGRET_GOAL, SELF_APOLOGIZE, SELF_SPOT_MY_PATTERNS, SELF_CHECK_CHANGE_GOAL, SELF_EXPRESS_NEW_PATTERN, and SELF_CHECK_FOLLOW_UP_GOAL, plus roughly 19 Q_*-prefixed decision-diamond nodes. index.html renders via mermaid.render() and discards bindFunctions; its #output click listener only toggles Q_* answer nodes and never calls preventDefault, so a mermaid `click NODE_ID \"url\"` link directive (rendered as a native SVG <a> wrapping the node) navigates normally with no index.html changes needed. server.js serves only \"/\", \"/api/diagrams\", \"/api/diagrams/:name\", and \"/api/watch/:name\"; there is no static-file route, so a new articles/ directory needs its own route in server.js before article pages are reachable. This task writes articles/communicate-new-pattern.html and adds `click SELF_EXPRESS_NEW_PATTERN \"articles/communicate-new-pattern.html\"` to accountability.mmd.",
    "modifiableFiles": [
      "diagrams/accountability.mmd",
      "articles/communicate-new-pattern.html"
    ],
    "readOnlyFiles": [
      "*"
    ],
    "createsFiles": [
      "articles/communicate-new-pattern.html"
    ],
    "schemaVersion": "1.0.1",
    "hasTests": true,
    "tests": "Click the block in the viewer and the article opens",
    "difficulty": 4,
    "blockedBy": [
      {
        "taskNumber": 4,
        "reason": "Needs the articles/ static route that the empathy article task adds to server.js"
      }
    ]
  },
  {
    "taskNumber": 11,
    "version": "c4b626354a9099657cd2e67c3c67777afcc7eb7e",
    "title": "Help article for the \"Goal: Follow-up\" block",
    "userDescription": "create a new task for each of the blocks that are the main goal points in the pipeline near the bottom, impact, empathy, change behavior, communication, there should be help articles that explain what those things are and what examples of them look like.",
    "chainGoal": [
      "- One help article per goal block in accountability.mmd, each linked from its block in the viewer"
    ],
    "goal": [
      "- The article says what the goal means in plain words",
      "- The article gives at least two real-life examples",
      "- The article gives an example of what it is NOT",
      "- The article is reachable from the block in the viewer"
    ],
    "notInScope": [
      "- Changes to the pipeline logic; the diagram stays as is",
      "- The mobile app; tasks 1 to 3 own it",
      "- Articles for the question diamonds; the question-diamonds article task owns them",
      "- The articles/ static route in server.js; task 4 owns it"
    ],
    "problemSolvedByTask": "The goal names are vague; people do not know what to actually do at each one",
    "description": "diagrams/accountability.mmd defines the goal blocks as node IDs SELF_CHECK_EMPATHY_GOAL, SELF_CHECK_IMPACT_GOAL, SELF_CHECK_REGRET_GOAL, SELF_APOLOGIZE, SELF_SPOT_MY_PATTERNS, SELF_CHECK_CHANGE_GOAL, SELF_EXPRESS_NEW_PATTERN, and SELF_CHECK_FOLLOW_UP_GOAL, plus roughly 19 Q_*-prefixed decision-diamond nodes. index.html renders via mermaid.render() and discards bindFunctions; its #output click listener only toggles Q_* answer nodes and never calls preventDefault, so a mermaid `click NODE_ID \"url\"` link directive (rendered as a native SVG <a> wrapping the node) navigates normally with no index.html changes needed. server.js serves only \"/\", \"/api/diagrams\", \"/api/diagrams/:name\", and \"/api/watch/:name\"; there is no static-file route, so a new articles/ directory needs its own route in server.js before article pages are reachable. This task writes articles/goal-follow-up.html and adds `click SELF_CHECK_FOLLOW_UP_GOAL \"articles/goal-follow-up.html\"` to accountability.mmd.",
    "modifiableFiles": [
      "diagrams/accountability.mmd",
      "articles/goal-follow-up.html"
    ],
    "readOnlyFiles": [
      "*"
    ],
    "createsFiles": [
      "articles/goal-follow-up.html"
    ],
    "schemaVersion": "1.0.1",
    "hasTests": true,
    "tests": "Click the block in the viewer and the article opens",
    "difficulty": 4,
    "blockedBy": [
      {
        "taskNumber": 4,
        "reason": "Needs the articles/ static route that the empathy article task adds to server.js"
      }
    ]
  },
  {
    "taskNumber": 12,
    "version": "28f6273f169619cdf885b23084f7537a77dc70eb",
    "title": "Help articles explaining the question diamonds and their order",
    "userDescription": "It would be good to have articles explaining why these question blocks have these specific questions and why they are in that order",
    "goal": [
      "- Each question diamond has an article section that says why that question is asked",
      "- The article explains why the questions come in that order",
      "- Each diamond is reachable to its section from the viewer"
    ],
    "notInScope": [
      "- Changes to the pipeline logic; the diagram stays as is",
      "- The mobile app; tasks 1 to 3 own it",
      "- Goal block articles; the per-block article tasks own them",
      "- The articles/ static route in server.js; task 4 owns it"
    ],
    "problemSolvedByTask": "The goal names are vague; people do not know what to actually do at each one",
    "description": "diagrams/accountability.mmd defines the goal blocks as node IDs SELF_CHECK_EMPATHY_GOAL, SELF_CHECK_IMPACT_GOAL, SELF_CHECK_REGRET_GOAL, SELF_APOLOGIZE, SELF_SPOT_MY_PATTERNS, SELF_CHECK_CHANGE_GOAL, SELF_EXPRESS_NEW_PATTERN, and SELF_CHECK_FOLLOW_UP_GOAL, plus roughly 19 Q_*-prefixed decision-diamond nodes. index.html renders via mermaid.render() and discards bindFunctions; its #output click listener only toggles Q_* answer nodes and never calls preventDefault, so a mermaid `click NODE_ID \"url\"` link directive (rendered as a native SVG <a> wrapping the node) navigates normally with no index.html changes needed. server.js serves only \"/\", \"/api/diagrams\", \"/api/diagrams/:name\", and \"/api/watch/:name\"; there is no static-file route, so a new articles/ directory needs its own route in server.js before article pages are reachable. This task writes articles/question-diamonds.html covering the Q_* diamonds and adds `click` directives on them. Implementation decision left open: one shared page with anchored sections per diamond, or one page per diamond.",
    "modifiableFiles": [
      "diagrams/accountability.mmd",
      "articles/question-diamonds.html"
    ],
    "readOnlyFiles": [
      "*"
    ],
    "createsFiles": [
      "articles/question-diamonds.html"
    ],
    "schemaVersion": "1.0.1",
    "hasTests": true,
    "tests": "Click a question diamond in the viewer and its article section opens",
    "difficulty": 6,
    "blockedBy": [
      {
        "taskNumber": 4,
        "reason": "Needs the articles/ static route that the empathy article task adds to server.js"
      }
    ]
  }
]
