{
  "name": "relationship-mermaid",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild viewer.ts --bundle --format=esm --outfile=viewer.js",
    "test": "npm run build && node --test \"tests/**/*.test.ts\""
  },
  "devDependencies": {
    "@types/node": "^26.5.1",
    "esbuild": "^0.28.2"
  },
  "dependencies": {
    "mermaid": "^12.0.0"
  }
}
